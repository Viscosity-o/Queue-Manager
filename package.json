{
  "name": "my-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Frontend/my-project && npm install && npm run build",
    "dev": "cd Frontend/my-project && npm install && npm run dev",
    "start": "cd Frontend/my-project && npm run dev"
  }
}
